#include "window.h"

#include <string>
#include <iostream>

int main() {
    leng::Window window("leng++", 800, 600);
    //window.create();
    std::string name;
    std::cin >> name;
    return 0;
}
